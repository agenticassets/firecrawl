# Optimized Domain Checker Settings
# Copy this to .env or set as environment variables

# Required: Your Firecrawl API key
FIRECRAWL_API_KEY=your-api-key-here

# Optional: Override defaults
FIRECRAWL_API_URL=http://localhost:3002
FIRECRAWL_SCRAPE_PATH=/v1/scrape

# OPTIMIZED DEFAULTS (already set in script, override if needed)
# --------------------------------------------------------------

# Concurrency: Number of parallel domain checks (default: 15)
# Safe range: 10-20 for local Docker setup
# Higher = faster but more resource intensive
DOMAIN_CHECK_CONCURRENCY=15

# Delay between requests in milliseconds (default: 100)
# Lower = faster, but be careful not to overwhelm the API
# 100ms with 15 concurrent = ~150 requests/second capacity
DOMAIN_CHECK_DELAY_MS=100

# How long to wait for page content to load (default: 5000ms)
DOMAIN_CHECK_WAITFOR_MS=5000

# Request timeout in milliseconds (default: 60000ms = 1 minute)
DOMAIN_CHECK_TIMEOUT_MS=60000

# Number of retries on failure (default: 2)
DOMAIN_CHECK_RETRIES=2

# Batch checkpoint: Save progress every N domains (default: 20)
# Lower = more frequent saves (safer but slightly slower I/O)
# Higher = less frequent saves (faster but lose more on crash)
DOMAIN_CHECK_BATCH_SIZE=20
